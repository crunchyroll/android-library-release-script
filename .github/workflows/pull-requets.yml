name: CI

on:
  pull_request:
    branches: [ master ]

jobs:
  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - name: SCM
        uses: actions/checkout@v2

      - name: Test
        uses: eskatos/gradle-command-action@v1
        with:
          arguments: test
